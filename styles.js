import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: "#fd0",
      alignItems: "center",
      justifyContent: "center",
    },
    bigText: {
      fontSize: 40,
      fontWeight: "700",
      color: 'red',
      textAlign: 'center'
    },
  });
  